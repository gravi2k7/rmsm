"use client";

/** Placeholder only (Task 2/8: layout must "support" a cookie banner, paired
 * with the Consent Provider). No consent-collection UI or logic yet — this
 * milestone only establishes where it will mount. */
export function CookieBanner() {
  return null;
}
