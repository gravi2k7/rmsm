/** Placeholder only (Task 2: public layout must "support" an announcement
 * bar). Renders nothing visible yet — a real dismissible/CMS-driven banner
 * is a later milestone. */
export function AnnouncementBar() {
  return null;
}
