server {
    listen 80;
    server_name localhost;

    location = /health {
        access_log off;
        return 200 "RMSM Gateway Online\n";
        add_header Content-Type text/plain;
    }

    location / {
        return 200 "RMSM Gateway Ready\n";
        add_header Content-Type text/plain;
    }
}