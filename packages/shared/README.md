# shared

Framework-agnostic shared TypeScript utilities, error types, constants.

_Status: scaffolded in Module 000. No application code yet — implementation begins in its owning module per the roadmap._
